rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /sessions/{shareId} {
      // Anyone with the session ID can read or write.
      // Documents are short-lived collaboration sessions — no auth needed.
      allow read, write: if true;
    }
  }
}
